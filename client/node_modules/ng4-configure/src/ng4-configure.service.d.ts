import { ConfigureOptions } from './configure-options';
import { Http } from '@angular/http';
import 'rxjs/add/operator/map';
import 'rxjs/add/operator/toPromise';
export declare class ConfigureService {
    private http;
    configOptions: ConfigureOptions;
    config: any;
    constructor(http: Http, configOptions: ConfigureOptions);
    load(): Promise<any>;
    private buildUrl();
    private makeId();
    private extractData(res);
}
